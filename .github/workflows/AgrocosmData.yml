name: AgrocosmData

on:
  push:
    branches:
      - main
    paths:
      - lib/AgrocosmData/**
      - .github/workflows/AgrocosmData.yml
  pull_request:
    paths:
      - lib/AgrocosmData/**
      - .github/workflows/AgrocosmData.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: Julia 1.10 - AgrocosmData
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: julia-actions/setup-julia@v2
        with:
          version: '1.10'
      - uses: julia-actions/cache@v2
        with:
          cache-compiled: false
          cache-scratchspaces: false
      - name: Run AgrocosmData tests
        run: julia --startup-file=no --project=lib/AgrocosmData -e 'using Pkg; Pkg.instantiate(); Pkg.test()'
